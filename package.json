{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --kill-others --names \"API,WEB\" --prefix-colors \"cyan,magenta\" \"pnpm --filter @workspace/api-server run dev\" \"cross-env PORT=5173 BASE_PATH=/ VITE_API_PROXY_TARGET=http://localhost:5000 pnpm --filter @workspace/isabella-ai run dev\"",
    "build": "pnpm run typecheck && pnpm --filter @workspace/api-server --filter @workspace/isabella-ai --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/api-server --filter @workspace/isabella-ai --filter ./scripts --if-present run typecheck",
    "test:agent": "node --experimental-strip-types --test scripts/agent-smoke.test.ts"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.3"
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "cross-env": "^7.0.3",
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
